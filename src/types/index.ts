export interface Produto {
  id: number;
  imagem: string;
  titulo: string;
  descricao: string;
}

export interface NavItem {
  id: string;
  titulo: string;
  link: string;
}

export interface SocialLink {
  id: string;
  icon: string;
  link: string;
  label: string;
} 